return  {
    messages = {
        KEY_EXISTS_IN_DB = "Key already exists in database",
        UNKNOWN_ERROR_CODE = "Unknown error",
        BROKEN_JSON = "Invalid JSON",
        KEY_NOT_EXISTS_IN_DB = "Key does not exists in database",
    },
    UNKNOWN_URI_RESPONSE = {status = 404, body = "Unknown path"},
    REQUEST_LIMIT_RESPONSE = {status = 429, body = "body"},
}